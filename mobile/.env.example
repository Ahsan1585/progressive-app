# Base URL of the existing Express backend (backend/index.js).
# Local dev matches the frontend app's default fallback of http://localhost:3000
# if this is left unset. Point this at the deployed backend URL in other environments.
VITE_API_URL=http://localhost:3000
